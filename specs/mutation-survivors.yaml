# Classified surviving mutants (SPECIFICATION.md §12.4). Human-owned (see CODEOWNERS).
#
# A surviving mutant is allowed ONLY if listed here with a classification of
#   equivalent-mutant | unreachable-defensive | tooling-limitation
# plus a rationale and human approval (`approved_by`). A `test-deficiency` classification, or an
# unlisted survivor, fails `run_mutation.py --require-kill-non-equivalent`: kill it with a test.
#
# key = "<module_path>::<operator_name>::<occurrence>"
#
# NOTE: tests/unit/tools/test_run_mutation.py::test_real_survivors_file_is_valid pins this
# file to ZERO entries — adding a real classification therefore requires a human-approved
# change that both adds the entry (with approved_by) and corrects that pin, citing the
# governing SPEC-ID. That is deliberate: no classification can exist without approval.
#
# PROPOSED (2026-07-16 retrospective audit, ADR 0010 — awaiting founder approval; these are
# comments, not entries). Keys confirmed by the final green-baseline run: 329 mutants,
# 322 killed; these SEVEN survivors are behaviourally unobservable by construction:
#
#   l7_settle/pnl.py::core/ReplaceComparisonOperator_Eq_Is::0        equivalent-mutant
#       `result == RunnerResult.UNKNOWN` vs `is` — Enum members are singletons
#   l7_settle/pnl.py::core/ReplaceComparisonOperator_Eq_Is::1        equivalent-mutant
#       `result == RunnerResult.LOSER` vs `is` — Enum singleton identity
#   l7_settle/ledger.py::core/ReplaceComparisonOperator_Gt_NotEq::0  equivalent-mutant
#       `v > e` vs `v != e` reached only after `v < e` raised; on v>=e they coincide
#   l7_settle/settlement.py::core/ReplaceComparisonOperator_Eq_Is::0 equivalent-mutant
#       `actual.result == RunnerResult.REMOVED` vs `is` — Enum singleton identity
#   l7_settle/settlement.py::core/ReplaceComparisonOperator_Eq_Is::2 equivalent-mutant
#       `market_status == MarketStatus.UNKNOWN` vs `is` — Enum singleton identity
#   l7_settle/settlement.py::core/ReplaceComparisonOperator_Eq_Is::3 equivalent-mutant
#       `result == RunnerResult.UNKNOWN` vs `is` — Enum singleton identity
#   l7_settle/settlement.py::core/ReplaceComparisonOperator_LtE_Lt::0 equivalent-mutant
#       `net <= 0` vs `< 0` differ only at net==0 where commission = round(rate*0) = 0
#   l7_settle/settlement.py::core/NumberReplacer::1                  equivalent-mutant
#       the 0 in `net <= 0` -> -1: differs only at net==0, same x0 identity
[]

# specs/gates — Versioned gate definitions

**Spec:** SPECIFICATION.md §10 · **Requirements:** SPEC-093 · **Human-owned** (see `CODEOWNERS`).

`v1.yaml` will define the pre-registered gate boundaries the deterministic evaluator reads.
Gate boundaries are deliberate, pre-registered, human-approved decisions — **not** values an
agent invents. Not authored here yet.

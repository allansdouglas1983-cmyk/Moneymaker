{
 "rules": [
  {
   "module": "sport_tennis/coherence/root_dedup.py",
   "definitions": ["connected_components", "deduplicate_roots"],
   "operator_prefixes": ["core/ReplaceBinaryOperator_BitOr_"],
   "proof_class": "POSTPONED_ANNOTATION_RUNTIME_INERT",
   "registered_domain_reachability": "The mutated `|` tokens are the union operators inside the parameter ANNOTATIONS `candidates: list[Root] | tuple[Root, ...]` (L77, L196). The module opens with `from __future__ import annotations` (PEP 563): every annotation is stored as a plain string and never evaluated at runtime.",
   "exact_reachable_state_evidence": "Under PEP 563 the annotation expression is compiled to a string constant; replacing BitOr with any operator changes only that string. No runtime code path evaluates it (no typing.get_type_hints call exists anywhere in the production tree for this module). Identical to the 66-annotation class ratified in the xmarket packet (MUTATION_SURVIVOR_PACKET_XMARKET_CONTRACTS_FINAL_V1).",
   "malformed_input_reachability": "n/a - annotations are never executed.",
   "output_fields_affected": "none",
   "pmf_comparison": "n/a",
   "serialization_digest_comparison": "byte-identical output for every input (verified by the agreement suite passing under the mutants).",
   "production_reference_comparison": "independent brute-force reference agreement holds for every fixture (the mutants change no executed instruction).",
   "associated_test_ids": "tests/unit/coherence/test_root_dedup.py (all); tests/unit/coherence/test_root_dedup_independent.py (all)",
   "proof_artifact_digest": "PEP 563 semantics; architecture-proof test precedent tests/unit/coherence/test_signatures.py"
  },
  {
   "module": "sport_tennis/coherence/root_dedup.py",
   "definitions": ["connected_components"],
   "operator_prefixes": ["core/ReplaceBinaryOperator_Add_", "core/NumberReplacer"],
   "proof_class": "EXACT_ALGEBRAIC_IDENTITY",
   "registered_domain_reachability": "L92 `for bi in range(ai + 1, len(order))` - the edge-pair enumeration start. Surviving variants (ai-1, ai*1, ai//1, ai%1, ai**1, ai>>1, ai|1, ai&1, ai^1, and NumberReplacer 1->0) all produce a start index <= ai+1.",
   "exact_reachable_state_evidence": "Every surviving variant enumerates a SUPERSET of the original index pairs. The extra pairs are self-pairs (have_edge(x,x) is True but union(find(i), find(i)) hits ri == rj and is skipped) or symmetric duplicates (the edge relation is symmetric and union is idempotent), so the union-find partition is IDENTICAL for every input. The one behaviour-changing sibling (1->2, which SKIPS adjacent pairs) was KILLED in round 1 by the reference partition-order comparison. Proof: max/union over a superset whose additional elements are fixed points.",
   "malformed_input_reachability": "n/a - indices are internal.",
   "output_fields_affected": "none - identical partition for all inputs.",
   "pmf_comparison": "n/a",
   "serialization_digest_comparison": "identical for all inputs.",
   "production_reference_comparison": "reference agreement (incl. partition order) passes under every surviving variant.",
   "associated_test_ids": "test_root_dedup_independent.py::test_all_permutations_of_bounded_sets_agree; ::test_randomized_deterministic_sets_agree",
   "proof_artifact_digest": "superset-of-fixed-points argument above; sibling-kill evidence in the round-1 diff"
  },
  {
   "module": "sport_tennis/coherence/root_dedup.py",
   "definitions": ["component_diameter"],
   "operator_prefixes": ["core/ReplaceBinaryOperator_Add_", "core/NumberReplacer"],
   "proof_class": "EXACT_ALGEBRAIC_IDENTITY",
   "registered_domain_reachability": "L111 `for b in component[i + 1:]` - the diameter pair enumeration. Surviving variants relax the slice start to <= i+1 (incl. NumberReplacer 1->0).",
   "exact_reachable_state_evidence": "A relaxed slice start yields a SUPERSET of the original pairs; the additions are self-pairs (Chebyshev distance 0) and symmetric duplicates (identical distances), so max() is unchanged for every component. The behaviour-changing sibling (1->2, skipping adjacent pairs and zeroing 2-member diameters) was KILLED in round 1 by the exact-diameter reference comparison. Proof: max over a superset whose additional elements never exceed the existing maximum (0 or duplicates).",
   "malformed_input_reachability": "n/a",
   "output_fields_affected": "none - identical diameters for all inputs.",
   "pmf_comparison": "n/a",
   "serialization_digest_comparison": "identical (diameter is serialized; the agreement suite compares it exactly).",
   "production_reference_comparison": "exact-diameter reference comparison passes under every surviving variant.",
   "associated_test_ids": "test_root_dedup_independent.py::agree (diameter comparison); test_root_dedup.py::test_component_and_diameter_seams",
   "proof_artifact_digest": "superset-max identity above; sibling-kill evidence in the round-1 diff"
  },
  {
   "module": "sport_tennis/coherence/root_dedup.py",
   "definitions": ["find"],
   "operator_prefixes": ["core/ReplaceComparisonOperator_NotEq_Lt"],
   "proof_class": "EXACT_ALGEBRAIC_IDENTITY",
   "registered_domain_reachability": "L87 `while parent[i] != i` in the union-find root walk.",
   "exact_reachable_state_evidence": "Invariant: parent[j] <= j at every step - initialisation sets parent[j] = j; union assigns parent[max(ri,rj)] = min(ri,rj) (strictly smaller); path-halving assigns parent[i] = parent[parent[i]] (values only decrease). Therefore parent[i] != i is exactly equivalent to parent[i] < i for every reachable state, and the mutant is behaviour-identical for all inputs.",
   "malformed_input_reachability": "n/a",
   "output_fields_affected": "none",
   "pmf_comparison": "n/a",
   "serialization_digest_comparison": "identical",
   "production_reference_comparison": "reference agreement passes under the mutant.",
   "associated_test_ids": "test_root_dedup_independent.py (all agreement suites)",
   "proof_artifact_digest": "monotone-parent invariant proof above"
  },
  {
   "module": "sport_tennis/coherence/root_dedup.py",
   "definitions": ["find"],
   "operator_prefixes": ["core/ReplaceComparisonOperator_NotEq_IsNot"],
   "proof_class": "IDEMPOTENT_NO_SIDE_EFFECT",
   "registered_domain_reachability": "L87 `while parent[i] != i` -> `is not`.",
   "exact_reachable_state_evidence": "If the caller-supplied index int is a distinct object from the stored parent int of equal value (possible for values outside CPython's small-int cache), the loop body executes exactly ONE extra iteration: parent[i] = parent[parent[i]] assigns parent[root] = parent[root] (a no-op) and i = parent[i] rebinds i to the STORED object; the next test compares the stored object with itself (`is` -> exit). The returned root and every parent entry are unchanged for every input - one idempotent extra iteration, no observable effect. For cached small ints the mutant is trivially identical.",
   "malformed_input_reachability": "n/a",
   "output_fields_affected": "none",
   "pmf_comparison": "n/a",
   "serialization_digest_comparison": "identical",
   "production_reference_comparison": "reference agreement passes under the mutant.",
   "associated_test_ids": "test_root_dedup_independent.py (all agreement suites)",
   "proof_artifact_digest": "single-extra-idempotent-iteration proof above"
  },
  {
   "module": "sport_tennis/coherence/root_dedup.py",
   "definitions": ["connected_components"],
   "operator_prefixes": ["core/ReplaceComparisonOperator_NotEq_IsNot"],
   "proof_class": "EXACT_ALGEBRAIC_IDENTITY",
   "registered_domain_reachability": "L97 `if ri != rj` -> `is not` (union guard).",
   "exact_reachable_state_evidence": "Reference-flow invariant: every value in `parent` originates from the SINGLE `list(range(len(cands)))` materialisation; unions assign min(ri,rj) (one of the two existing objects) and path-halving copies existing references. find() returns elements of `parent`, so ri and rj are always objects drawn from that one materialisation - equal value implies the SAME object. `!=` and `is not` therefore coincide for every reachable state regardless of candidate count.",
   "malformed_input_reachability": "n/a",
   "output_fields_affected": "none",
   "pmf_comparison": "n/a",
   "serialization_digest_comparison": "identical",
   "production_reference_comparison": "reference agreement passes under the mutant.",
   "associated_test_ids": "test_root_dedup_independent.py (all agreement suites)",
   "proof_artifact_digest": "single-materialisation reference-flow proof above"
  },
  {
   "module": "sport_tennis/coherence/root_dedup.py",
   "definitions": ["ambiguous"],
   "operator_prefixes": ["core/ReplaceComparisonOperator_Gt_"],
   "proof_class": "EXACT_ALGEBRAIC_IDENTITY",
   "registered_domain_reachability": "L169 `len(self.ambiguous_components) > 0` -> `!= 0`.",
   "exact_reachable_state_evidence": "len() is non-negative by construction, so `> 0` and `!= 0` are exactly equivalent on every reachable state.",
   "malformed_input_reachability": "n/a",
   "output_fields_affected": "none",
   "pmf_comparison": "n/a",
   "serialization_digest_comparison": "identical",
   "production_reference_comparison": "reference agreement passes under the mutant.",
   "associated_test_ids": "test_root_dedup.py::test_chain_refuses_identically_for_every_permutation; ::test_empty_input_yields_empty_result",
   "proof_artifact_digest": "non-negative-length identity"
  }
 ]
}
